CREATE table Wetter(
                         id int auto_increment,
                         country varchar(60),
                         temperature varchar(15),
                         time varchar(80),
                         primary key(id)
);
